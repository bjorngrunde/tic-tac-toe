<?php

/**
 * @var \App\Views\LeaderboardView $this
 * @see \App\Controllers\IndexController::chartAjaxAction()
 */
?>

<div class="row content-container col-xs-12">
    <div class="container-fluid">
        <div class="row">
            <h3>Bracket size: <?= $this->grid_size; ?> columns</h3>
        </div>
        <div class="row">
            <h3>Total players in this bracket: <?= $this->playerCount; ?></h3>
        </div>
    </div>
    <table class="table">
        <thead>
            <tr>
                <td>Rank</td>
                <td>Name</td>
                <td>Time</td>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($this->players as $player) { ?>
                <tr>
                    <td><?= ++$this->rank; ?></td>
                    <td><?= $player['name']; ?></td>
                    <td><?= $this->showSeconds((int) $player['play_time_seconds']); ?> seconds</td>
                </tr>
            <?php } ?>
        </tbody>
    </table>
</div>